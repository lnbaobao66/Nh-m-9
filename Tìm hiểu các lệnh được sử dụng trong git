Git là một hệ thống quản lý phiên bản phân tán (distributed version control system) và cung cấp một số lệnh để làm việc với repository. Dưới đây là một số lệnh phổ biến trong Git:
git init: Khởi tạo một repository Git mới trong thư mục hiện tại.
git clone <url>: Sao chép (clone) một repository từ một URL xa vào máy tính của bạn.
git add <file>: Thêm một file vào vùng staging (staging area) để chuẩn bị cho commit.
git commit -m "<message>": Tạo một commit mới với các thay đổi đã được thêm vào staging area và gắn kèm một thông điệp.
git status: Hiển thị trạng thái hiện tại của repository, bao gồm các file đã thay đổi và các file chưa được theo dõi.
git log: Hiển thị lịch sử commit của repository.
git push: Đẩy (push) các commit mới lên repository từ máy tính của bạn lên một remote repository.
git pull: Kéo (pull) các thay đổi mới nhất từ remote repository về máy tính của bạn.
git branch: Liệt kê tất cả các nhánh (branches) hiện có trong repository.
git checkout <branch>: Chuyển đổi sang một nhánh khác trong repository.
git merge <branch>: Kết hợp (merge) các thay đổi từ một nhánh khác vào nhánh hiện tại.
git remote add <name> <url>: Thêm một remote repository với tên và URL tương ứng.
git remote -v: Hiển thị danh sách các remote repository đã được cấu hình.
git diff: Hiển thị sự khác biệt giữa các thay đổi chưa được staging và commit cuối cùng.
git reset <file>: Hủy bỏ (unstage) một file đã được thêm vào staging area.
git rm <file>: Xóa một file khỏi repository và staging area.
Đây chỉ là một số lệnh cơ bản trong Git. Git cung cấp nhiều lệnh khác để làm việc với repository và quản lý lịch sử commit. Bạn có thể tìm hiểu thêm về các lệnh khác và các tùy chọn của chúng trong tài liệu hướng dẫn Git.
